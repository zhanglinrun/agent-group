<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linrun.infrastructure.dao.IGroupBuyOrderLockDao">

    <resultMap id="GroupBuyTeamMap" type="com.linrun.infrastructure.po.GroupBuyTeamPO">
        <id column="id" property="id"/>
        <result column="team_id" property="teamId"/>
        <result column="activity_id" property="activityId"/>
        <result column="goods_id" property="goodsId"/>
        <result column="target_count" property="targetCount"/>
        <result column="complete_count" property="completeCount"/>
        <result column="lock_count" property="lockCount"/>
        <result column="team_status" property="teamStatus"/>
        <result column="valid_start_time" property="validStartTime"/>
        <result column="valid_end_time" property="validEndTime"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <resultMap id="GroupBuyOrderLockMap" type="com.linrun.infrastructure.po.GroupBuyOrderLockPO">
        <id column="id" property="id"/>
        <result column="lock_id" property="lockId"/>
        <result column="idempotent_key" property="idempotentKey"/>
        <result column="user_id" property="userId"/>
        <result column="team_id" property="teamId"/>
        <result column="order_id" property="orderId"/>
        <result column="activity_id" property="activityId"/>
        <result column="goods_id" property="goodsId"/>
        <result column="lock_amount" property="lockAmount"/>
        <result column="lock_status" property="lockStatus"/>
        <result column="lock_time" property="lockTime"/>
    </resultMap>

    <resultMap id="GroupBuyTeamDetailMap" type="com.linrun.infrastructure.po.GroupBuyTeamDetailPO">
        <result column="user_id" property="userId"/>
        <result column="team_id" property="teamId"/>
        <result column="activity_id" property="activityId"/>
        <result column="target_count" property="targetCount"/>
        <result column="complete_count" property="completeCount"/>
        <result column="lock_count" property="lockCount"/>
        <result column="valid_start_time" property="validStartTime"/>
        <result column="valid_end_time" property="validEndTime"/>
        <result column="out_trade_no" property="outTradeNo"/>
    </resultMap>

    <resultMap id="GroupBuyTeamStatisticMap" type="com.linrun.infrastructure.po.GroupBuyTeamStatisticPO">
        <result column="all_team_count" property="allTeamCount"/>
        <result column="all_team_complete_count" property="allTeamCompleteCount"/>
        <result column="all_team_user_count" property="allTeamUserCount"/>
    </resultMap>

    <insert id="insertTeam" parameterType="com.linrun.infrastructure.po.GroupBuyTeamPO">
        insert into group_buy_team (
          team_id,
          activity_id,
          goods_id,
          target_count,
          complete_count,
          lock_count,
          team_status,
          valid_start_time,
          valid_end_time,
          create_time
        ) values (
          #{teamId},
          #{activityId},
          #{goodsId},
          #{targetCount},
          #{completeCount},
          #{lockCount},
          #{teamStatus},
          #{validStartTime},
          #{validEndTime},
          #{createTime}
        )
    </insert>

    <update id="updateTeamLockCount">
        update group_buy_team
        set lock_count = lock_count + 1
        where team_id = #{teamId}
          and team_status = 'PROCESSING'
          and lock_count &lt; target_count
          and (valid_end_time is null or valid_end_time &gt; now())
    </update>

    <update id="updateOrderLockPaid">
        update group_buy_order_lock
        set lock_status = 'PAID'
        where order_id = #{orderId}
          and lock_status = 'LOCKED'
    </update>

    <update id="updateTeamCompleteCount">
        update group_buy_team
        set complete_count = complete_count + 1,
            team_status = case
                            when complete_count + 1 &gt;= target_count then 'SUCCESS'
                            else team_status
                          end
        where team_id = #{teamId}
          and team_status = 'PROCESSING'
          and complete_count &lt; target_count
    </update>

    <insert id="insertOrderLock" parameterType="com.linrun.infrastructure.po.GroupBuyOrderLockPO">
        insert into group_buy_order_lock (
          lock_id,
          idempotent_key,
          user_id,
          team_id,
          order_id,
          activity_id,
          goods_id,
          lock_amount,
          lock_status,
          lock_time
        ) values (
          #{lockId},
          #{idempotentKey},
          #{userId},
          #{teamId},
          #{orderId},
          #{activityId},
          #{goodsId},
          #{lockAmount},
          #{lockStatus},
          #{lockTime}
        )
    </insert>

    <select id="queryLockByIdempotentKey" resultMap="GroupBuyOrderLockMap">
        select id,
               lock_id,
               idempotent_key,
               user_id,
               team_id,
               order_id,
               activity_id,
               goods_id,
               lock_amount,
               lock_status,
               lock_time
        from group_buy_order_lock
        where idempotent_key = #{idempotentKey}
        limit 1
    </select>

    <select id="queryLockByOrderId" resultMap="GroupBuyOrderLockMap">
        select id,
               lock_id,
               idempotent_key,
               user_id,
               team_id,
               order_id,
               activity_id,
               goods_id,
               lock_amount,
               lock_status,
               lock_time
        from group_buy_order_lock
        where order_id = #{orderId}
        limit 1
    </select>

    <select id="queryTeamByTeamId" resultMap="GroupBuyTeamMap">
        select id,
               team_id,
               activity_id,
               goods_id,
               target_count,
               complete_count,
               lock_count,
               team_status,
               valid_start_time,
               valid_end_time,
               create_time
        from group_buy_team
        where team_id = #{teamId}
        limit 1
    </select>

    <select id="queryPaidOrderIdsByTeamId" resultType="java.lang.String">
        select order_id
        from group_buy_order_lock
        where team_id = #{teamId}
          and lock_status = 'PAID'
    </select>

    <update id="releaseLockedOrder">
        update group_buy_order_lock
        set lock_status = 'RELEASED'
        where order_id = #{orderId}
          and lock_status = 'LOCKED'
    </update>

    <update id="releasePaidOrder">
        update group_buy_order_lock
        set lock_status = 'RELEASED'
        where order_id = #{orderId}
          and lock_status = 'PAID'
    </update>

    <update id="reduceTeamLockCount">
        update group_buy_team
        set lock_count = greatest(lock_count - 1, 0)
        where team_id = #{teamId}
    </update>

    <update id="reduceTeamPaidCount">
        update group_buy_team
        set lock_count = greatest(lock_count - 1, 0),
            complete_count = greatest(complete_count - 1, 0)
        where team_id = #{teamId}
    </update>

    <select id="queryTimeoutUnsettledPaidOrderIds" resultType="java.lang.String">
        select l.order_id
        from group_buy_order_lock l
        inner join group_buy_team t on t.team_id = l.team_id
        where l.lock_status = 'PAID'
          and t.team_status = 'PROCESSING'
          and t.valid_end_time is not null
          and t.valid_end_time &lt;= #{deadline}
        order by l.id asc
        limit #{limit}
    </select>

    <select id="queryTimeoutUnsettledLockedOrderIds" resultType="java.lang.String">
        select l.order_id
        from group_buy_order_lock l
        inner join group_buy_team t on t.team_id = l.team_id
        where l.lock_status = 'LOCKED'
          and t.team_status = 'PROCESSING'
          and t.valid_end_time is not null
          and t.valid_end_time &lt;= #{deadline}
        order by l.id asc
        limit #{limit}
    </select>

    <select id="countUserActivityLocks" resultType="int">
        select count(1)
        from group_buy_order_lock
        where user_id = #{userId}
          and activity_id = #{activityId}
          and lock_status in ('LOCKED', 'PAID')
    </select>

    <select id="queryInProgressOwnerTeamDetails" resultMap="GroupBuyTeamDetailMap">
        select l.user_id,
               t.team_id,
               t.activity_id,
               t.target_count,
               t.complete_count,
               t.lock_count,
               t.valid_start_time,
               t.valid_end_time,
               coalesce(p.out_trade_no, l.idempotent_key) as out_trade_no
        from group_buy_order_lock l
        inner join group_buy_team t on t.team_id = l.team_id
        left join pay_order p on p.order_id = l.order_id
        where t.activity_id = #{activityId}
          and l.user_id = #{userId}
          and t.team_status = 'PROCESSING'
          and (t.valid_end_time is null or t.valid_end_time &gt; now())
          and l.lock_status in ('LOCKED', 'PAID')
        order by t.id desc
        limit #{limit}
    </select>

    <select id="queryInProgressRandomTeamDetails" resultMap="GroupBuyTeamDetailMap">
        select '' as user_id,
               t.team_id,
               t.activity_id,
               t.target_count,
               t.complete_count,
               t.lock_count,
               t.valid_start_time,
               t.valid_end_time,
               '' as out_trade_no
        from group_buy_team t
        where t.activity_id = #{activityId}
          and t.team_status = 'PROCESSING'
          and (t.valid_end_time is null or t.valid_end_time &gt; now())
          and (#{userId} is null or not exists (
              select 1
              from group_buy_order_lock l
              where l.team_id = t.team_id
                and l.user_id = #{userId}
                and l.lock_status in ('LOCKED', 'PAID')
          ))
        order by t.id desc
        limit #{limit}
    </select>

    <select id="queryTeamStatisticByActivityId" resultMap="GroupBuyTeamStatisticMap">
        select count(1) as all_team_count,
               sum(case when team_status = 'SUCCESS' then 1 else 0 end) as all_team_complete_count,
               coalesce(sum(complete_count), 0) as all_team_user_count
        from group_buy_team
        where activity_id = #{activityId}
    </select>
</mapper>
