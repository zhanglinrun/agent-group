<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linrun.reactor.infrastructure.dao.reactor.IVisitorIdentityDao">

    <resultMap id="VisitorIdentityMap" type="com.linrun.reactor.infrastructure.dao.po.VisitorIdentityPO">
        <id column="id" property="id"/>
        <result column="visitor_id" property="visitorId"/>
        <result column="token_digest" property="tokenDigest"/>
        <result column="status" property="status"/>
        <result column="first_seen_at" property="firstSeenAt"/>
        <result column="last_seen_at" property="lastSeenAt"/>
        <result column="last_ip" property="lastIp"/>
        <result column="last_user_agent" property="lastUserAgent"/>
        <result column="username" property="username"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <insert id="insert" parameterType="com.linrun.reactor.infrastructure.dao.po.VisitorIdentityPO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO visitor_identity (
            visitor_id, token_digest, status, first_seen_at, last_seen_at, last_ip, last_user_agent, username, deleted
        ) VALUES (
            #{visitorId}, #{tokenDigest}, #{status}, #{firstSeenAt}, #{lastSeenAt}, #{lastIp}, #{lastUserAgent}, #{username}, #{deleted}
        )
    </insert>

    <select id="queryByTokenDigest" parameterType="java.lang.String" resultMap="VisitorIdentityMap">
        SELECT * FROM visitor_identity
        WHERE token_digest = #{tokenDigest} AND deleted = 0
        LIMIT 1
    </select>

    <select id="queryByVisitorId" parameterType="java.lang.String" resultMap="VisitorIdentityMap">
        SELECT * FROM visitor_identity
        WHERE visitor_id = #{visitorId} AND deleted = 0
        LIMIT 1
    </select>

    <update id="updateLastSeen">
        UPDATE visitor_identity SET
            last_seen_at = #{lastSeenAt},
            last_ip = #{lastIp},
            last_user_agent = #{lastUserAgent}
        WHERE visitor_id = #{visitorId} AND deleted = 0
    </update>

    <update id="bindUsernameIfAbsent">
        UPDATE visitor_identity SET
            username = #{username},
            update_time = #{updateTime}
        WHERE visitor_id = #{visitorId} AND (username IS NULL OR username = '') AND deleted = 0
    </update>

</mapper>
