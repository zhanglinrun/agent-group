server:
  port: ${SERVER_PORT:8080}
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  application:
    name: agent-group
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:}

agent:
  user-model:
    crypto-secret: ${AGENT_USER_MODEL_CRYPTO_SECRET:}
